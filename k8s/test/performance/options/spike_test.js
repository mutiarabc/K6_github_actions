export const options = {
  stages: [
    { duration: `${__ENV.VUS}`, target: 3 },
    { duration: `${__ENV.VUS}`, target: 3 },
    { duration: `${__ENV.VUS}`, target: 20 },
    { duration: `${__ENV.VUS}`, target: 20 },
    { duration: `${__ENV.VUS}`, target: 0 },
  ]
};


